:mod:`!symtable` --- Access to the compiler's symbol tables
===========================================================

.. module:: symtable
   :synopsis: Interface to the compiler's internal symbol tables.

**Source code:** :source:`Lib/symtable.py`

--------------

Symbol tables are generated by the compiler from AST just before bytecode is
generated.  The symbol table is responsible for calculating the scope of every
identifier in the code.  :mod:`!symtable` provides an interface to examine these
tables.


Generating Symbol Tables
------------------------

.. function:: symtable(code, filename, compile_type, *, module=None)
              symtable(code: str, filename: str, compile_type: str) -> symtable.SymbolTable

   (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L12>`__)

   Return the toplevel :class:`SymbolTable` for the Python source *code*.
   *filename* is the name of the file containing the code.  *compile_type* is
   like the *mode* argument to :func:`compile`.
   The optional argument *module* specifies the module name.
   It is needed to unambiguous :ref:`filter <warning-filter>` syntax warnings
   by module name.

   .. versionadded:: 3.15
      Added the *module* parameter.


Examining Symbol Tables
-----------------------

.. class:: SymbolTableType
           SymbolTableType(value: typing.Any, names: None = None) -> _EnumMemberT
           SymbolTableType(value: str, names: _EnumNames, *, module: str | None = None, qualname: str | None = None, type: type[Any] | None = None, start: int = 1, boundary: enum.FlagBoundary | None = None) -> type[enum.Enum]
           SymbolTableType(value: typing.Any, *values: typing.Any) -> _EnumMemberT

   (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L17>`__)

   An enumeration indicating the type of a :class:`SymbolTable` object.

   .. attribute:: MODULE
      :type: typing.Literal[symtable.SymbolTableType.MODULE]
      :value: "module"

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L18>`__)

      Used for the symbol table of a module.

   .. attribute:: FUNCTION
      :type: typing.Literal[symtable.SymbolTableType.FUNCTION]
      :value: "function"

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L19>`__)

      Used for the symbol table of a function.

   .. attribute:: CLASS
      :type: typing.Literal[symtable.SymbolTableType.CLASS]
      :value: "class"

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L20>`__)

      Used for the symbol table of a class.

   The following members refer to different flavors of
   :ref:`annotation scopes <annotation-scopes>`.

   .. attribute:: ANNOTATION
      :type: typing.Literal[symtable.SymbolTableType.ANNOTATION]
      :value: "annotation"

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L21>`__)

      Used for annotations if ``from __future__ import annotations`` is active.

   .. attribute:: TYPE_ALIAS
      :type: typing.Literal[symtable.SymbolTableType.TYPE_ALIAS]
      :value: "type alias"

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L22>`__)

      Used for the symbol table of :keyword:`type` constructions.

   .. attribute:: TYPE_PARAMETERS
      :type: typing.Literal[symtable.SymbolTableType.TYPE_PARAMETERS]
      :value: "type parameters"

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L23>`__)

      Used for the symbol table of :ref:`generic functions <generic-functions>`
      or :ref:`generic classes <generic-classes>`.

   .. attribute:: TYPE_VARIABLE
      :type: typing.Literal[symtable.SymbolTableType.TYPE_VARIABLE]
      :value: "type variable"

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L24>`__)

      Used for the symbol table of the bound, the constraint tuple or the
      default value of a single type variable in the formal sense, i.e.,
      a TypeVar, a TypeVarTuple or a ParamSpec object (the latter two do
      not support a bound or a constraint tuple).

   .. versionadded:: 3.13

.. class:: SymbolTable
           SymbolTable(raw_table: typing.Any, filename: str) -> symtable.SymbolTable

   (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L26>`__)

   A namespace table for a block.  The constructor is not public.

   .. method:: get_type()
               get_type() -> symtable.SymbolTableType

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L29>`__)

      Return the type of the symbol table.  Possible values are members
      of the :class:`SymbolTableType` enumeration.

      .. versionchanged:: 3.12
         Added ``'annotation'``,  ``'TypeVar bound'``, ``'type alias'``,
         and ``'type parameter'`` as possible return values.

      .. versionchanged:: 3.13
         Return values are members of the :class:`SymbolTableType` enumeration.

         The exact values of the returned string may change in the future,
         and thus, it is recommended to use :class:`SymbolTableType` members
         instead of hard-coded strings.

   .. method:: get_id()
               get_id() -> int

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L33>`__)

      Return the table's identifier.

   .. method:: get_name()
               get_name() -> str

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L34>`__)

      Return the table's name.  This is the name of the class if the table is
      for a class, the name of the function if the table is for a function, or
      ``'top'`` if the table is global (:meth:`get_type` returns ``'module'``).
      For type parameter scopes (which are used for generic classes, functions,
      and type aliases), it is the name of the underlying class, function, or
      type alias. For type alias scopes, it is the name of the type alias.
      For :class:`~typing.TypeVar` bound scopes, it is the name of the ``TypeVar``.

   .. method:: get_lineno()
               get_lineno() -> int

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L35>`__)

      Return the number of the first line in the block this table represents.

   .. method:: is_optimized()
               is_optimized() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L36>`__)

      Return ``True`` if the locals in this table can be optimized.

   .. method:: is_nested()
               is_nested() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L37>`__)

      Return ``True`` if the block is a nested class or function.

   .. method:: has_children()
               has_children() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L38>`__)

      Return ``True`` if the block has nested namespaces within it.  These can
      be obtained with :meth:`get_children`.

   .. method:: get_identifiers()
               get_identifiers() -> collections.abc.dict_keys[str, int]

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L39>`__)

      Return a view object containing the names of symbols in the table.
      See the :ref:`documentation of view objects <dict-views>`.

   .. method:: lookup(name)
               lookup(name: str) -> symtable.Symbol

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L40>`__)

      Lookup *name* in the table and return a :class:`Symbol` instance.

   .. method:: get_symbols()
               get_symbols() -> list[symtable.Symbol]

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L41>`__)

      Return a list of :class:`Symbol` instances for names in the table.

   .. method:: get_children()
               get_children() -> list[symtable.SymbolTable]

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L42>`__)

      Return a list of the nested symbol tables.


.. class:: Function
           Function(raw_table: typing.Any, filename: str) -> symtable.Function

   (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L44>`__)

   A namespace for a function or method.  This class inherits from
   :class:`SymbolTable`.

   .. method:: get_parameters()
               get_parameters() -> tuple[str, ...]

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L45>`__)

      Return a tuple containing names of parameters to this function.

   .. method:: get_locals()
               get_locals() -> tuple[str, ...]

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L46>`__)

      Return a tuple containing names of locals in this function.

   .. method:: get_globals()
               get_globals() -> tuple[str, ...]

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L47>`__)

      Return a tuple containing names of globals in this function.

   .. method:: get_nonlocals()
               get_nonlocals() -> tuple[str, ...]

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L49>`__)

      Return a tuple containing names of explicitly declared nonlocals in this function.

   .. method:: get_frees()
               get_frees() -> tuple[str, ...]

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L48>`__)

      Return a tuple containing names of :term:`free (closure) variables <closure variable>`
      in this function.

   .. method:: get_cells()

      Return a tuple containing names of :term:`cell variables <closure variable>` in this table.

      .. versionadded:: 3.15


.. class:: Class
           Class(raw_table: typing.Any, filename: str) -> symtable.Class

   (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L51>`__)

   A namespace of a class.  This class inherits from :class:`SymbolTable`.

   .. method:: get_methods()
               get_methods() -> tuple[str, ...]

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L54>`__)

      Return a tuple containing the names of method-like functions declared
      in the class.

      Here, the term 'method' designates *any* function defined in the class
      body via :keyword:`def` or :keyword:`async def`.

      Functions defined in a deeper scope (e.g., in an inner class) are not
      picked up by :meth:`get_methods`.

      For example:

      .. testsetup:: symtable.Class.get_methods

         import warnings
         context = warnings.catch_warnings()
         context.__enter__()
         warnings.simplefilter("ignore", category=DeprecationWarning)

      .. testcleanup:: symtable.Class.get_methods

         context.__exit__()

      .. doctest:: symtable.Class.get_methods

         >>> import symtable
         >>> st = symtable.symtable('''
         ... def outer(): pass
         ...
         ... class A:
         ...    def f():
         ...        def w(): pass
         ...
         ...    def g(self): pass
         ...
         ...    @classmethod
         ...    async def h(cls): pass
         ...
         ...    global outer
         ...    def outer(self): pass
         ... ''', 'test', 'exec')
         >>> class_A = st.get_children()[2]
         >>> class_A.get_methods()
         ('f', 'g', 'h')

      Although ``A().f()`` raises :exc:`TypeError` at runtime, ``A.f`` is still
      considered as a method-like function.

      .. deprecated-removed:: 3.14 3.16


.. class:: Symbol
           Symbol(name: str, flags: int, namespaces: collections.abc.Sequence[symtable.SymbolTable] | None = None, *, module_scope: bool = False) -> symtable.Symbol

   (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L58>`__)

   An entry in a :class:`SymbolTable` corresponding to an identifier in the
   source.  The constructor is not public.

   .. method:: get_name()
               get_name() -> str

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L63>`__)

      Return the symbol's name.

   .. method:: is_referenced()
               is_referenced() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L64>`__)

      Return ``True`` if the symbol is used in its block.

   .. method:: is_imported()
               is_imported() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L77>`__)

      Return ``True`` if the symbol is created from an import statement.

   .. method:: is_parameter()
               is_parameter() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L65>`__)

      Return ``True`` if the symbol is a parameter.

   .. method:: is_type_parameter()
               is_type_parameter() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L67>`__)

      Return ``True`` if the symbol is a type parameter.

      .. versionadded:: 3.14

   .. method:: is_global()
               is_global() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L69>`__)

      Return ``True`` if the symbol is global.

   .. method:: is_nonlocal()
               is_nonlocal() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L62>`__)

      Return ``True`` if the symbol is nonlocal.

   .. method:: is_declared_global()
               is_declared_global() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L70>`__)

      Return ``True`` if the symbol is declared global with a global statement.

   .. method:: is_local()
               is_local() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L71>`__)

      Return ``True`` if the symbol is local to its block.

   .. method:: is_annotated()
               is_annotated() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L72>`__)

      Return ``True`` if the symbol is annotated.

      .. versionadded:: 3.6

   .. method:: is_free()
               is_free() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L73>`__)

      Return ``True`` if the symbol is referenced in its block, but not assigned
      to.

   .. method:: is_cell()

      Return ``True`` if the symbol is referenced but not assigned in a nested block.

      .. versionadded:: 3.15

   .. method:: is_free_class()
               is_free_class() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L75>`__)

      Return *True* if a class-scoped symbol is free from
      the perspective of a method.

      Consider the following example::

         def f():
             x = 1  # function-scoped
             class C:
                 x = 2  # class-scoped
                 def method(self):
                     return x

      In this example, the class-scoped symbol ``x`` is considered to
      be free from the perspective of ``C.method``, thereby allowing
      the latter to return *1* at runtime and not *2*.

      .. versionadded:: 3.14

   .. method:: is_assigned()
               is_assigned() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L78>`__)

      Return ``True`` if the symbol is assigned to in its block.

   .. method:: is_comp_iter()
               is_comp_iter() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L80>`__)

      Return ``True`` if the symbol is a comprehension iteration variable.

      .. versionadded:: 3.14

   .. method:: is_comp_cell()
               is_comp_cell() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L81>`__)

      Return ``True`` if the symbol is a cell in an inlined comprehension.

      .. versionadded:: 3.14

   .. method:: is_namespace()
               is_namespace() -> bool

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L83>`__)

      Return ``True`` if name binding introduces new namespace.

      If the name is used as the target of a function or class statement, this
      will be true.

      For example::

         >>> table = symtable.symtable("def some_func(): pass", "string", "exec")
         >>> table.lookup("some_func").is_namespace()
         True

      Note that a single name can be bound to multiple objects.  If the result
      is ``True``, the name may also be bound to other objects, like an int or
      list, that does not introduce a new namespace.

   .. method:: get_namespaces()
               get_namespaces() -> collections.abc.Sequence[symtable.SymbolTable]

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L84>`__)

      Return a list of namespaces bound to this name.

   .. method:: get_namespace()
               get_namespace() -> symtable.SymbolTable

      (`typeshed <https://github.com/python/typeshed/blob/282b1a8/stdlib/symtable.pyi#L85>`__)

      Return the namespace bound to this name. If more than one or no namespace
      is bound to this name, a :exc:`ValueError` is raised.


.. _symtable-cli:

Command-Line Usage
------------------

.. versionadded:: 3.13

The :mod:`!symtable` module can be executed as a script from the command line.

.. code-block:: sh

   python -m symtable [infile...]

Symbol tables are generated for the specified Python source files and
dumped to stdout.
If no input file is specified, the content is read from stdin.
